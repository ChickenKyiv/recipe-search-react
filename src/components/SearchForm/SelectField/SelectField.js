import React from 'react';
// @TODO should we move part of render into this component from SelectContainer?
// not sure...
const SelectField = (props) => {
  return (

  );
};

export default SelectField;
